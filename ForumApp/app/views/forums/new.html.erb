<h1>Create New Forum</h1>
<%= simple_form_for @forum do |f| %>
  <%= render "shared/error_messages", object: @forum %>

  <%= f.hidden_field :user_id, value: cookies[:token] %>
  <%= f.input :name, label: "Forum Name: " %>
  <%= f.submit "Create Forum" %>
<% end %>